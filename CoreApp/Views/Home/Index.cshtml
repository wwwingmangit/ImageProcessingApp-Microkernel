@model CoreApp.Models.PluginSelectionModel
@{
    ViewData["Title"] = "Home";
}

<h2>Upload Image and Select Plugin</h2>

<form asp-action="ProcessImage" method="post" enctype="multipart/form-data">
    <div>
        <label for="file">Select an image to upload:</label>
        <input type="file" name="file" id="file" accept="image/*" required />
    </div>
    <div>
        <label for="pluginSelect">Select a plugin:</label>
        <select name="pluginName" id="pluginSelect" required>
            <optgroup label="Image Plugins">
                @foreach (var plugin in Model.ImagePlugins)
                {
                    <option value="@plugin.Name">@plugin.Name</option>
                }
            </optgroup>
            <optgroup label="Text Plugins">
                @foreach (var plugin in Model.TextPlugins)
                {
                    <option value="@plugin.Name">@plugin.Name</option>
                }
            </optgroup>
        </select>
    </div>
    <div>
        <button type="submit">Process Image</button>
    </div>
</form>
